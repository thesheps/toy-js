describe("Toy", () => {
  it("looks like TypeScript is correctly configured", () => {
    class MyLovelyClass {
      thing: string;

      constructor(thing: string) {
        this.thing = thing;
      }
    }
  });
});
